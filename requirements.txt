optuna>=3.6
optunahub>=0.4
sentence-transformers>=2.7
scikit-learn>=1.4
numpy>=1.26
pandas>=2.2

# Optional: ML-based evaluation gates (src/evaluation/medium_gate.py)
# Install for aesthetic/quality scoring and CLIP-based alignment/novelty checks.
# Without these, MediumGate passes with a warning and skips ML scoring.
#
# pyiqa>=0.1.12          # 50+ IQA metrics (NIMA, BRISQUE, MUSIQ, TOPIQ, etc.)
# torch>=2.0             # Required by pyiqa
# torchvision>=0.15      # Required by pyiqa
# clip-by-openai>=1.0    # CLIP ViT-B/32 / ViT-L/14 for text-image alignment
# Pillow>=10.0           # Image loading for medium gate
#
# To install all optional evaluation deps at once:
#   pip install pyiqa torch torchvision clip-by-openai Pillow
sqlite-vec>=0.1.6
networkx>=3.3

# ---------------------------------------------------------------------------
# Phase 8: Monitoring & Observability
# ---------------------------------------------------------------------------

# Required for Rich-powered dashboard fallback (strongly recommended)
rich>=13.0

# Optional: Full Textual TUI dashboard (install for the best live experience)
# textual>=0.80

# Optional: matplotlib charts in analyze_run.py
# matplotlib>=3.8
